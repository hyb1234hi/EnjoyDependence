apply plugin: 'groovy'
apply plugin: 'maven'
apply from: "./publish.gradle"

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile gradleApi()
    compile localGroovy()
}

repositories {
    mavenCentral()
}